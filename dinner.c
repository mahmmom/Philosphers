#include "philo.h"

 void   dinner_init(t_data *data)
 {
    if (0 == data->num_meals_limit)
        return ;
    else if (1 == data->philo_num)
        {//implement
        }
    else
        thread_handle(pthread_t *thread, void *(*foo)(void *), void *data, t_opcode opcode)
 }
